using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace CommandModule
{
    public enum MenuCommand { None, Save, OpenFile, Create, SaveAs, CloseFile,Print,PreView };
    public enum ProgramCommand { None, OpenTxt, Exit, Minim, ExitAll, OpenFm, Change }
    class Program
    {
        static void Main()
        {
        }    
    }
    
}
